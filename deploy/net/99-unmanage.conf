# Hand wlan0 + eth0 to systemd-networkd / hostapd; keep NetworkManager off them.
# Installed to /etc/NetworkManager/conf.d/99-unmanage.conf
[keyfile]
unmanaged-devices=interface-name:wlan0;interface-name:eth0
